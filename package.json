{
  "name": "docker-node-mongo-typescript",
  "version": "1.0.0",
  "description": "Use MongoDB with Docker to create Node service with TypeScript",
  "main": "src/index.ts",
  "scripts": {
    "start": "dev:live",
    "dev:live": "nodemon --watch 'src/**/*.ts' --exec \"ts-node\" src/index.ts",
    "build": "rimraf ./dist && tsc",
    "build:ts": "tsc -p ."
  },
  "keywords": [
    "docker",
    "node",
    "express",
    "mogodb",
    "typescript"
  ],
  "author": "harry huang",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.3",
    "mongoose": "^5.2.7"
  },
  "devDependencies": {
    "@types/mongoose": "^5.7.36",
    "@types/node": "^14.6.4",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  }
}
